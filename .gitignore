# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# uv
uv.lock.bak

# Ambiente / segredos
.env
.env.local
*/.env.local
infra/terraform/terraform.tfvars

# Terraform — state contém segredos; .terraform/ é cache local
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
infra/terraform/backend.hcl
# Saída de `terraform plan` — binário, não versionar
tfplan
*.tfplan

# Dados locais
data/
*.duckdb
warehouse/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# Frontend (Next.js)
node_modules/
dist/
apps/dashboard/web/out/
apps/dashboard/web/.next/
apps/dashboard/web/next-env.d.ts
apps/dashboard/web/tsconfig.tsbuildinfo

# IDE / OS
.DS_Store
.idea/
.vscode/
# Docs de desenvolvimento — não versionar (handoff, runbook, guia do agente, notas de sessão)
HANDOFF*.md
RUNBOOK*.md
CLAUDE.md

# Notas locais de scratch (memória persistente real fica em ~/.claude/)
memory/

# Script de produção local — não versionar
tools/prod-freeze.sh
